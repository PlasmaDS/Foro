spring:
  profile.active: dev, test, prod
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/foro
    username: root
    password: root

server:
  error:
    include-stacktrace: never
